import * as React from 'react'
import { DirectionProvider as BaseDirectionProvider } from '@base-ui-components/react/direction-provider'

export const DirectionProvider = BaseDirectionProvider

// Also export the useDirection hook
export { useDirection } from '@base-ui-components/react/direction-provider'
